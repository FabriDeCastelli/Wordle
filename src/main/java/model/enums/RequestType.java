package model.enums;

/**
 * Represents a request that can be sent to the server.
 */
public enum RequestType {

    LOGIN,
    REGISTER,
    LOGOUT,
    PLAY,
    SENDWORD,
    SHARE,
    SENDMESTATISTICS,
    SHOWMESHARING,

}
